{% extends 'base.html.twig' %}

{% block body %}
  
   <div class="container mt-4">
   <h1>See all products</h1>
<div class="card mb-4">
<div class="row">

  {% for product in products %}
  <div class="col-md-4">
 <div class="card-body">
  {% if product.image is not null %}
     <img class="thumbnail" src="{{ asset(product.image.webPath) }}"  width="100px"; height="100px"; />
     
  {% endif %}
  {#<div class="card-body">#}
    <h5 class="card-title">
      <a href={{ path('product_show', {'slug': product.slug}) }}>
        {{ product.name }}
      </a>
    </h5>
    
    <div class="text-primary" style="font-size:2rem; font-weight: bold;">
      {{ product.price }} €
    </div>
  </div>
</div>
{% endfor %}
</div>
</div>
</div>
{% endblock %}
